language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t juju08217/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true juju08217/docker-react npm run test